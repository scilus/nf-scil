process {
    withName: "RECONST_FREEWATER" {
        publishDir = { "${params.outdir}/${task.process.tokenize(':')[-1].tokenize('_')[0].toLowerCase()}" }
        ext.para_diff = 1.7e-3
        ext.perp_diff_min = 0.1e-3
        ext.perp_diff_max = 0.7e-3
        ext.iso_diff = 3e-3
        ext.lambda1 = 0.5
        ext.lambda2 = 1e-3
        ext.b_thr = 40
        ext.nb_threads = 1
    }
}