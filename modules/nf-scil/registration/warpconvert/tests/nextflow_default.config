process {
    withName: "REGISTRATION_WARPCONVERT" {
        publishDir = { "${params.outdir}/${task.process.tokenize(':')[-1].tokenize('_')[0].toLowerCase()}" }
        ext.invert = "true"
        ext.downsample = "true"
    }
}
