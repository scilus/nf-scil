process {
    publishDir = { "${params.outdir}/${task.process.tokenize(':')[-1].tokenize('_')[0].toLowerCase()}" }
    ext.robust = true
    ext.fast = true
    ext.output_vol = true
    ext.output_qc = true
    ext.resample = true
    ext.crop = 64
}
